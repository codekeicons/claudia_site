---
import blinker from "@/styles/blinker.module.css";

interface itemNavbar {
    path: string;
    text: string;
}

const { path, text } = Astro.props as itemNavbar;
---

<a
    href={path}
    class={`block group text-sm ${blinker.regular} lg:text-xl linkTextColor montserrat-regular py-2 px-3 text-dark md:text-white rounded md:p-0  relative w-fit after:block after:content-[''] after:absolute after:h-[3px] after:bg-white after:w-full after:scale-x-0 after:hover:scale-x-100 after:transition after:duration-300 after:origin-center`}
    >{text}</a
>
